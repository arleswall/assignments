function eachLetter(str) {
    for (var i = 0; i < str.length; i++) {
     console.log(str[i]);
    }
}

eachLetter("Arles");

function findCharacter (character) {
    for (var i = 0; i < character.length); i++) {
        console.log(character.indexOf(character));
    }
}

findCharacter(r);